#include <bits/stdc++.h>
using namespace std;

int main()
{
    list<int> ls;
    // same operations like vector but can directy push front
    ls.push_front(1); //insert funstion in a vector is time consuming and costly 

    //same functions like vector
    return 0;
}