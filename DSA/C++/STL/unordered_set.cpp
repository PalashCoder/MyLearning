#include <bits/stdc++.h>
using namespace std;

int main()
{
    // all like set but not sorted but elements are unique and upper bound and lower bound does not work here
    // all operations are O(1)
    unordered_set<int> us;

    return 0;
}